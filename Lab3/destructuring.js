//for object
let myObj = {
    name : "Meet",
    age : 19,
    rollNo : 661
};

let {name, age} = myObj;

console.log(`Your name is ${name} and your age is ${age}`);


//for array
let arr = ['1','2','3','4','5'];

let [num1, num2] = arr;

console.log(`Number 1 from arr is ${num1} and Number 2 from arr is ${num2}`);
